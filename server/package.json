{
  "name": "delivery-proof-server",
  "version": "1.0.0",
  "main": "app.js",
  "scripts": {
    "prestart": "powershell -Command \"try { $pids = (netstat -ano | Select-String '0.0.0.0:5000|:::5000' | ForEach-Object { $_.ToString().Trim().Split()[-1] } | Sort-Object -Unique); foreach ($p in $pids) { if ($p -match '^\\d+$') { taskkill /F /PID $p 2>$null } } } catch {}\"",
    "start": "node app.js",
    "dev": "node app.js"
  },
  "dependencies": {
    "axios": "^1.13.5",
    "bcryptjs": "^3.0.3",
    "cloudinary": "^2.9.0",
    "cors": "^2.8.6",
    "dotenv": "^17.3.1",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "multer": "^2.1.0",
    "mysql2": "^3.17.4",
    "socket.io": "^4.8.3",
    "stripe": "^20.3.1",
    "uuid": "^13.0.0"
  }
}
